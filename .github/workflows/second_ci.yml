name: Second CI

on:
  schedule:
    - cron: '0 */24 * * *' # Every 24 hours

jobs:
  Second_CI:
    name: Second CI
    runs-on: ubuntu-latest
    
    steps:
      - run: echo "Started!"
      - name: Checkout
        uses: actions/checkout@v3.3.0
      - run: echo "This repo is ${{ github.repository }}..."
      - name: Run update script
        run: sh ./backup/start.sh
        shell: sh
